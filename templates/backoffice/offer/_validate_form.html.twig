

<form method="post" action="{{ path('app_backoffice_offer_validate', {'id': offer.id}) }}" onsubmit="return confirm('Valider cette annonce ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('validate' ~ offer.id) }}">
    <button class="btn btn-success">Valider</button>
</form>

