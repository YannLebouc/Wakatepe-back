
<form method="post" action="{{ path('app_backoffice_offer_delete', {'id': offer.id}) }}" onsubmit="return confirm('Supprimer cette annonce ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offer.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
