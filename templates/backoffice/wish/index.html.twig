{% extends 'base.html.twig' %}

{% block title %}Wish index{% endblock %}

{% block body %}
    <h1>Wish index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Zipcode</th>
                <th>Picture</th>
                <th>Description</th>
                <th>IsActive</th>
                <th>Type</th>
                <th>IsReported</th>
                <th>CreatedAt</th>
                <th>UpdatedAt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for wish in wishes %}
            <tr>
                <td>{{ wish.id }}</td>
                <td>{{ wish.title }}</td>
                <td>{{ wish.zipcode }}</td>
                <td>{{ wish.picture }}</td>
                <td>{{ wish.description }}</td>
                <td>{{ wish.isActive ? 'Yes' : 'No' }}</td>
                <td>{{ wish.type }}</td>
                <td>{{ wish.isReported ? 'Yes' : 'No' }}</td>
                <td>{{ wish.createdAt ? wish.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ wish.updatedAt ? wish.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_backoffice_wish_show', {'id': wish.id}) }}">show</a>
                    <a href="{{ path('app_backoffice_wish_edit', {'id': wish.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="11">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_backoffice_wish_new') }}">Create new</a>
{% endblock %}
