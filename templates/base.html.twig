<!DOCTYPE html>
<html>
	<head>
		<meta
		charset="UTF-8">

		<!-- Bootstrap CSS -->
		<link
		href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-F3w7mX95PdgyTmZZMECAngseQB83DfGTowi0iMjiWaeVhAn4FJkqJByhZMI3AhiU" crossorigin="anonymous">
		<!-- Bootstrap icons -->
		<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css">

		<title>
			{% block title %}BackOffice-O'troc
			{% endblock %}
		</title>

		<link
		rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
		{# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
		{% block stylesheets %}
			{{ encore_entry_link_tags('app') }}
		{% endblock %}

		{% block javascripts %}
			{{ encore_entry_script_tags('app') }}
		{% endblock %}
	</head>
	<body>
		<div class="container-fluid"> 
        	<nav class="navbar navbar-expand-lg navbar-dark">
				<ul class="navbar-nav me-auto mb-2 mb-lg-0">
					<li>
						<a href="{{ path('app_backoffice_main')}}">
							<span>O'troc\</span>
						</a>
					</li>
					<li>
						<a href="{{ path('app_backoffice_maincategory_index')}}">
							<span>MainCatégories\</span>
						</a>
					</li>
					<li>
						<a href="{{ path('app_backoffice_category_index')}}">
							<span>Catégories\</span>
						</a>
					</li>
					<li>
						<a href="{{ path('app_backoffice_offer_index')}}">
							<span>Offres\</span>
						</a>
					</li>
                    <li>
						<a href="{{ path('app_backoffice_wish_index')}}">
							<span>Demandes\</span>
						</a>
					</li>
                    <li>
						<a href="{{ path('app_backoffice_user_index')}}">
							<span>Utilisateurs\</span>
						</a>
					</li>
					<li>
						<a href="{{ path('app_backoffice_reported')}}">
							<span>Annonces reportées\</span>
						</a>
					</li>
				{% if is_granted('IS_AUTHENTICATED_FULLY') %}
					<li>
						<a href="{{path('app_logout')}}" {# type="button" class="btn btn-danger me-2" #}>
							<span>Deconnexion\</span>
						</a>
					</li>
				{% else %}
					<li>
						<a href="{{path('app_login')}}" {# type="button" class="btn btn-danger me-2" #}>
							<span>Connexion\</span>
						</a>
					</li>
				{% endif %}
					{# <li>
						<a href="{{ path('app_main_test')}}">
							<span>Rechercher\</span>
						</a>
					</li> #}
				</ul>
				<div id="right">
					{% if is_granted('IS_AUTHENTICATED_FULLY') %}
						<span class="mr-2">Bienvenue
							<b>{{ app.user.alias }}</b>
						</span>
						{# <a href="{{ path('question_add') }}" class="btn btn-danger">Poser une question</a>
                        {% if is_granted('ROLE_MODERATOR') %}
                        <a href="{{ path('tag_index') }}" class="btn btn-success">Tags</a>
                        <a href="{{ path('admin_user') }}" class="btn btn-success">Utilisateurs</a>
                        {% endif %} #}
						{# <a href="{{ path('user_profile') }}" class="btn btn-primary">Mon compte</a> #}
						{# <a href="{{ path('logout') }}" class="btn btn-secondary">Déconnexion</a> #}
					{% else %}
						<span class="mr-2">Bienvenue invité</span>
						{# <a href="{{ path('user_register') }}" class="btn btn-success">Inscription</a> #}
						{# <a href="{{ path('login') }}" class="btn btn-primary">Connexion</a> #}
					{% endif %}

				</div>
			</nav>
            {% block body %}{% endblock %}
            <nav class="navbar navbar-expand-lg navbar-dark">
				<ul class="navbar-nav me-auto mb-2 mb-lg-0">
					<li>
						<a href="{{ path('app_backoffice_main')}}">
							<span>O'troc\</span>
						</a>
					</li>
					<li>
						<a href="{{ path('app_backoffice_maincategory_index')}}">
							<span>MainCatégories\</span>
						</a>
					</li>
					<li>
						<a href="{{ path('app_backoffice_category_index')}}">
							<span>Catégories\</span>
						</a>
					</li>
					<li>
						<a href="{{ path('app_backoffice_offer_index')}}">
							<span>Offres\</span>
						</a>
					</li>
                    <li>
						<a href="{{ path('app_backoffice_wish_index')}}">
							<span>Demandes\</span>
						</a>
					</li>
                    <li>
						<a href="{{ path('app_backoffice_user_index')}}">
							<span>Utilisateurs\</span>
						</a>
					</li>
					<li>
						<a href="{{ path('app_backoffice_reported')}}">
							<span>Annonces reportées\</span>
						</a>
					</li>
				{% if is_granted('IS_AUTHENTICATED_FULLY') %}
					<li>
						<a href="{{path('app_logout')}}" {# type="button" class="btn btn-danger me-2" #}>
							<span>Deconnexion\</span>
						</a>
					</li>
				{% else %}
					<li>
						<a href="{{path('app_login')}}" {# type="button" class="btn btn-danger me-2" #}>
							<span>Connexion\</span>
						</a>
					</li>
				{% endif %}

					{# <li>
						<a href="{{ path('app_main_test')}}">
							<span>Rechercher\</span>
						</a>
					</li> #}
				</ul>
				<div id="right">
					{% if is_granted('IS_AUTHENTICATED_FULLY') %}
						<span class="mr-2">Bienvenue
							<b>{{ app.user.alias }}</b>
						</span>
						{# <a href="{{ path('question_add') }}" class="btn btn-danger">Poser une question</a>
                        {% if is_granted('ROLE_MODERATOR') %}
                        <a href="{{ path('tag_index') }}" class="btn btn-success">Tags</a>
                        <a href="{{ path('admin_user') }}" class="btn btn-success">Utilisateurs</a>
                        {% endif %} #}
						{# <a href="{{ path('user_profile') }}" class="btn btn-primary">Mon compte</a> #}
						{# <a href="{{ path('logout') }}" class="btn btn-secondary">Déconnexion</a> #}
					{% else %}
						<span class="mr-2">Bienvenue invité</span>
						{# <a href="{{ path('user_register') }}" class="btn btn-success">Inscription</a> #}
						{# <a href="{{ path('login') }}" class="btn btn-primary">Connexion</a> #}
					{% endif %}

				</div>
			</nav>
		</div>
	</body>
</html>
